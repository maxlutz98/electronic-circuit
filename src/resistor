
public class resistor extends component{ // represents a resistor in a curcuit

    public resistor (){// default constructor
         }

    public void calculateThisImpedance(){
        this.setRe(this.getValue());
    }
}
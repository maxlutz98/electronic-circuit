
public class inductivity extends component{ // represents a inductivity in a curcuit

    public inductivity(){//default constructor
        }

    public void calculateThisImpedance(){
        float temp_Impedance = ((this.getFreq() * 2 * PI) * this.getValue());
        this.setIm(temp_Impedance);
    }

}